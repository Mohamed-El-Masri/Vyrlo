export class SearchSuggestions {
    constructor(inputId, type) {
        this.input = document.getElementById(inputId);
        this.type = type;
        this.setupSuggestions();
    }

    setupSuggestions() {
        // ... سأكمل هذا الجزء إذا أردت
    }
}
